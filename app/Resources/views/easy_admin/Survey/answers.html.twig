<div class="survey-answer">
	{% for answer in value %}
		<a href="{{ path('answer_show', {id: answer.id}) }}">
			{{ answer.createdAt | date }}
			{{ answer.id }}
		</a>
	{% else %}
		{{ 'No answers' | trans }}
	{% endfor %}
</div>
