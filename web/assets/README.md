# Building style sheet and scripts

First, install required `npm` packages:

```sh
npm config set engine-strict true
npm install
```

Then run `gulp` to generate style sheets and scripts:

```sh
gulp
```
